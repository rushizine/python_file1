name : Basic commands

  on:
        schedule:
          -corn : "*/2 * * *" #Every day at midnight UTC
          workflow_distpatch: #Manual trigger

          jobs:
            demo-commands:
            runs-on: ubuntu-latest

            steps:
              -name: show current working directory
              run: pwd
